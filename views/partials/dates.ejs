<% dates.forEach(function(date) { %>
    <div id="<%= date.dat_id %>" class="datediv border-2 border-dark grid grid-cols-6">
        <div class="col-span-2 bg-dark text-text p-2 text-xl text-center content-center flex">
            <p><%= date.dat_date %></p>
        </div>
        <div class="col-span-4">
            <p class="text-text text-lg m-1"><%= date.dat_description %></p>
            <div class="justify-between flex m-1">
                <button id="zusage<%= date.dat_id %>" name="<%= date.dat_id %>" onclick="commit('zusage',this.name)" class="border border-dark border-opacity-30 text-text hover:bg-dark p-1 mx-1">Zusagen</button>
                <button id="unsicher<%= date.dat_id %>" name="<%= date.dat_id %>" onclick="commit('unsicher',this.name)" class="border border-dark border-opacity-30 text-text hover:bg-dark p-1 mx-1">Unsicher</button>
                <button id="absage<%= date.dat_id %>" name="<%= date.dat_id %>" onclick="commit('absage',this.name)" class="border border-dark border-opacity-30 text-text hover:bg-dark p-1 mx-1">Absagen</button>
            </div>
        </div>
    </div>
<% }); %>